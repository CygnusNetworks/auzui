{
  "name": "auzui-workspace",
  "private": true,
  "version": "0.0.0",
  "description": "auzui monorepo workspace root — A Usable Zabbix UI",
  "license": "AGPL-3.0-only",
  "packageManager": "pnpm@11.20.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "lint": "pnpm -r lint",
    "build": "pnpm -r build",
    "test": "pnpm -r test"
  }
}
